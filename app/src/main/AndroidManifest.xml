<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"git
        android:icon="@drawable/ic_sos"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_sos"
        android:supportsRtl="true"
        android:theme="@style/Theme.CampusSos">

        <activity
            android:name=".app.EmergencyConfirmActivity"
            android:exported="false" />

        <activity
            android:name=".app.PermissionExplanationActivity"
            android:exported="false" />

        <activity
            android:name=".app.SettingsActivity"
            android:exported="false" />

        <activity
            android:name=".app.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.React.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".app.MainActivity"
            android:exported="false" />

        <service
            android:name=".emergency.EmergencyDispatchService"
            android:exported="false"
            android:foregroundServiceType="shortService" />

        <receiver
            android:name=".widget.SosWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/sos_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.SosWidgetReceiver"
            android:exported="false" />

        <receiver
            android:name=".sms.SmsStatusReceiver"
            android:exported="false" />

        <receiver
            android:name=".boot.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
